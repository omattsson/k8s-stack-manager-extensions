apiVersion: v1
kind: Namespace
metadata:
  name: extensions
---
apiVersion: v1
kind: Secret
metadata:
  name: slack-notifier
  namespace: extensions
type: Opaque
stringData:
  SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/REPLACE_ME"
  SLACK_WEBHOOK_SECRET: "REPLACE_ME"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-notifier
  namespace: extensions
  labels:
    app: slack-notifier
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slack-notifier
  template:
    metadata:
      labels:
        app: slack-notifier
    spec:
      containers:
        - name: slack-notifier
          image: slack-notifier:latest
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: slack-notifier
          env:
            - name: STACK_MANAGER_URL
              value: "https://stack-manager.example"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: slack-notifier
  namespace: extensions
spec:
  selector:
    app: slack-notifier
  ports:
    - port: 8080
      targetPort: 8080
