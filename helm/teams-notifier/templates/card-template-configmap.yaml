{{- if .Values.cardTemplate }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "teams-notifier.fullname" . }}-card-template
  labels:
    {{- include "teams-notifier.labels" . | nindent 4 }}
data:
  card-template.json: |
    {{- .Values.cardTemplate | nindent 4 }}
{{- end }}
